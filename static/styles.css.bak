* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, Helvetica, sans-serif;
    min-height: 100vh;
    background: linear-gradient(135deg, #0f172a, #1e293b, #334155);
    color: #0f172a;
}

.auth-page {
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 24px;
}

.auth-card {
    width: 100%;
    max-width: 900px;
    background: #ffffff;
    border-radius: 20px;
    padding: 36px 32px;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.25);
}

.brand-block {
    margin-bottom: 28px;
    text-align: center;
}

.brand-tag {
    font-size: 0.9rem;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: #2563eb;
    margin-bottom: 10px;
}

.brand-block h1 {
    font-size: 2rem;
    margin-bottom: 10px;
    color: #0f172a;
}

.auth-subtext {
    font-size: 0.98rem;
    line-height: 1.5;
    color: #475569;
}

.auth-form {
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.form-input {
    width: 100%;
    padding: 14px 15px;
    border: 1px solid #cbd5e1;
    border-radius: 12px;
    font-size: 1rem;
    background: #f8fafc;
    color: #0f172a;
    transition: border-color 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
}

.form-input:focus {
    outline: none;
    border-color: #2563eb;
    background: #ffffff;
    box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.18);
}

.form-input::placeholder {
    color: #94a3b8;
}

.auth-button {
    width: 100%;
    border: none;
    border-radius: 12px;
    padding: 14px;
    font-size: 1rem;
    font-weight: 700;
    cursor: pointer;
    background: #2563eb;
    color: #ffffff;
    transition: background 0.2s ease, transform 0.15s ease;
}

.auth-button:hover {
    background: #1d4ed8;
}

.auth-button:active {
    transform: translateY(1px);
}

.auth-button:focus {
    outline: none;
    box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.2);
}

.auth-switch {
    text-align: center;
    font-size: 0.95rem;
    color: #475569;
}

.auth-switch a {
    color: #2563eb;
    font-weight: 700;
    text-decoration: none;
}

.auth-switch a:hover, .auth-switch a:focus {
    text-decoration: underline;
}

.error-text {
    font-size: 0.9rem;
    color: #b91c1c;
    font-weight: 600;
}

@media (max-width: 800px){.auth-card {
        max-width: 95%;
        padding: 28px 20px;
    }.brand-block h1 {
        font-size: 1.7rem;
    }.features {
        grid-template-columns: 1fr;
    }}.feature {
    background: #f8fafc;
    padding: 14px;
    border-radius: 12px;
}

.home-buttons {
    margin-top: 30px;
    display: flex;
    gap: 12px;
    flex: 1;
    flex-direction: column;
}

.app-layout {
    display: flex;
    min-height: 100vh;
    background: #f3f4f6;
}

.sidebar {
    width: 240px;
    background: #0f2747;
    color: #ffffff;
    padding: 22px 14px;
    display: flex;
    flex-direction: column;
    flex-shrink: 0;
}

.sidebar-brand {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 30px;
}

.brand-icon {
    width: 42px;
    height: 42px;
    border-radius: 10px;
    background: #f59e0b;
    color: #0f2747;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
    font-size: 1.2rem;
}

.sidebar-brand h2 {
    font-size: 1.35rem;
    margin-bottom: 2px;
    color: #ffffff;
}

.sidebar-brand p {
    font-size: 0.82rem;
    color: #cbd5e1;
}

.sidebar-nav {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.nav-heading {
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: #94a3b8;
    margin: 10px 8px 4px;
    font-weight: 700;
}

.nav-spacing {
    margin-top: 20px;
}

.nav-link {
    text-decoration: none;
    color: #e2e8f0;
    padding: 12px 14px;
    border-radius: 12px;
    font-weight: 600;
    transition: background 0.2s ease, color 0.2s ease;
}

.nav-link:hover, .nav-link:focus {
    background: rgba(255, 255, 255, 0.08);
    color: #ffffff;
}

.nav-link.active {
    background: rgba(245, 158, 11, 0.18);
    color: #fbbf24;
}

.main-content {
    flex: 1;
    padding: 28px 32px;
    min-width: 0;
}

.topbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
    margin-bottom: 26px;
    flex-wrap: wrap;
    background: transparent;
    color: inherit;
    padding: 0;
}

.topbar h1 {
    font-size: 2.2rem;
    color: #0f172a;
    margin: 0 0 8px 0;
    line-height: 1.1;
}

.topbar-subtext {
    color: #64748b;
    font-size: 1rem;
    margin: 0;
}

.topbar-right {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 14px;
    flex-wrap: wrap;
    margin-left: auto;
}

.user-pill {
    width: 42px;
    height: 42px;
    border-radius: 999px;
    background: #f59e0b;
    color: #111827;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
    flex-shrink: 0;
}

.user-name {
    font-weight: 600;
    color: #0f172a;
    white-space: nowrap;
}

.primary-action {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    background: #f59e0b;
    color: #111827;
    font-weight: 700;
    padding: 12px 18px;
    border-radius: 12px;
    transition: background 0.2s ease;
    white-space: nowrap;
}

.primary-action:hover, .primary-action:focus {
    background: #d97706;
}

.accent-orange {
    border-top-color: #f59e0b;
}

.accent-blue {
    border-top-color: #2563eb;
}

.accent-green {
    border-top-color: #10b981;
}

.accent-dark {
    border-top-color: #0f2747;
}

.stat-icon {
    font-size: 1.6rem;
    margin-bottom: 12px;
}

.dashboard-grid {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 18px;
}

.panel {
    background: #ffffff;
    border-radius: 16px;
    box-shadow: 0 4px 18px rgba(15, 23, 42, 0.08);
    overflow: hidden;
    padding: 24px;
}

.large-panel {
    min-height: 320px;
}

.full-width {
    width: 100%;
}

.panel-link {
    text-decoration: none;
    color: #2563eb;
    font-weight: 700;
}

.pr-list,
.macro-list,
.session-list,
.table-wrapper {
    padding: 20px 22px;
}

.pr-row,
.macro-row,
.session-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.pr-row {
    padding: 16px 0;
    border-bottom: 1px solid #e2e8f0;
    color: #1e293b;
}

.pr-row:last-child,
.session-item:last-child {
    border-bottom: none;
}

.macro-row {
    margin-bottom: 8px;
    color: #1e293b;
    font-weight: 600;
}

.progress-bar {
    width: 100%;
    height: 12px;
    background: #e5e7eb;
    border-radius: 999px;
    overflow: hidden;
    margin-bottom: 18px;
}

.progress-fill {
    height: 100%;
    background: #2563eb;
    border-radius: 999px;
}

.session-item {
    padding: 16px 0;
    border-bottom: 1px solid #e2e8f0;
}

.session-item h4 {
    color: #0f172a;
    margin-bottom: 4px;
}

.session-item p {
    color: #64748b;
    font-size: 0.94rem;
}

.session-minutes {
    color: #64748b;
    font-weight: 700;
    font-size: 0.92rem;
}

.data-table th, .data-table td {
    text-align: left;
    padding: 14px 12px;
    border-bottom: 1px solid #e2e8f0;
    color: #1e293b;
}

.data-table th {
    color: #475569;
    font-size: 0.86rem;
    text-transform: uppercase;
    letter-spacing: 0.6px;
}

.table-link {
    text-decoration: none;
    color: #2563eb;
    font-weight: 700;
}

.empty-state {
    color: #64748b;
    font-size: 0.98rem;
    padding: 20px 0;
}

@media (max-width: 1100px){.stats-grid {
        grid-template-columns: repeat(2, 1fr);
    }.dashboard-grid {
        grid-template-columns: 1fr;
    }}@media (max-width: 800px){.app-layout {
        flex-direction: column;
    }.sidebar {
        width: 100%;
    }.main-content {
        padding: 20px;
    }.topbar {
        flex-direction: column;
        align-items: flex-start;
    }.topbar-right {
        width: 100%;
        justify-content: flex-start;
        margin-left: 0;
    }.stats-grid {
        grid-template-columns: 1fr;
    }.data-table {
        font-size: 0.9rem;
    }}.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 20px;
    margin-bottom: 24px;
}

.stat-card {
    background: #ffffff;
    border: 1px solid #e5e7eb;
    border-radius: 16px;
    padding: 20px;
    box-shadow: 0 8px 24px rgba(15, 23, 42, 0.06);
}

.stat-label {
    margin: 0 0 8px 0;
    font-size: 0.9rem;
    color: #6b7280;
}

.stat-card h2 {
    margin: 0;
    font-size: 1.9rem;
    line-height: 1.1;
}

.panel.full-width {
    width: 100%;
    margin-bottom: 24px;
}

.table-wrapper {
    width: 100%;
    overflow-x: auto;
}

.data-table {
    width: 100%;
    border-collapse: collapse;
    min-width: 720px;
}

.data-table thead th {
    text-align: left;
    padding: 14px 16px;
    font-size: 0.9rem;
    border-bottom: 1px solid #e5e7eb;
    color: #475569;
    background: #f8fafc;
}

.data-table tbody td {
    padding: 14px 16px;
    border-bottom: 1px solid #eef2f7;
    vertical-align: middle;
}

.data-table tbody tr:hover {
    background: #f8fafc;
}

.table-badge {
    display: inline-block;
    padding: 6px 10px;
    border-radius: 999px;
    background: #eef2ff;
    color: #3730a3;
    font-size: 0.8rem;
    font-weight: 600;
}

.empty-table {
    text-align: center;
    color: #6b7280;
    padding: 24px;
}

.panel-header {
    margin-bottom: 14px;
}

.panel-header h3 {
    margin: 0;
}

.meal-actions {
    margin-top: 6px;
    font-size: 0.9rem;
}

.delete-button {
    background: none;
    border: none;
    padding: 0;
    margin: 0;
    font: inherit;
    cursor: pointer;
    color: #b91c1c;
}

.delete-button:hover {
    text-decoration: underline;
}

.prediction-description {
    margin-bottom: 24px;
    color: #6b7280;
    line-height: 1.6;
}

.prediction-form {
    display: flex;
    flex-direction: column;
    gap: 18px;
    margin-top: 20px;
}

.form-group {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

.form-group label {
    font-weight: 700;
    color: #111827;
}

.form-control {
    width: 100%;
    max-width: 360px;
    padding: 12px 14px;
    border: 1px solid #d1d5db;
    border-radius: 12px;
    font-size: 1rem;
    background: white;
    color: #111827;
}

.form-control:focus {
    outline: none;
    border-color: #f97316;
    box-shadow: 0 0 0 3px rgba(249, 115, 22, 0.15);
}

.form-actions {
    display: flex;
    align-items: center;
    gap: 12px;
}

.error-box {
    margin-top: 24px;
    padding: 16px 18px;
    border-radius: 14px;
    background: #fef2f2;
    border: 1px solid #fecaca;
    color: #991b1b;
}

.error-box p {
    margin-top: 6px;
}

.prediction-stats {
    margin-top: 20px;
    margin-bottom: 24px;
}

.warning-box {
    margin-top: 24px;
    padding: 16px 18px;
    border-radius: 14px;
    background: #fffbeb;
    border: 1px solid #fde68a;
    color: #92400e;
}

.warning-box p {
    margin-top: 6px;
    line-height: 1.6;
}

.prediction-chart-card {
    margin-top: 24px;
    padding: 22px;
    border-radius: 18px;
    background: #ffffff;
    border: 1px solid #e5e7eb;
}

.strength-chart {
    display: flex;
    flex-direction: column;
    gap: 18px;
    margin-top: 20px;
}

.chart-row {
    display: grid;
    grid-template-columns: 90px 1fr 90px;
    align-items: center;
    gap: 14px;
}

.chart-label {
    font-weight: 700;
    color: #374151;
}

.chart-value {
    font-weight: 800;
    color: #111827;
    text-align: right;
}

.chart-bar-track {
    height: 18px;
    border-radius: 999px;
    background: #e5e7eb;
    overflow: hidden;
}

.chart-bar {
    height: 100%;
    border-radius: 999px;
}

.current-bar {
    background: #2563eb;
}

.predicted-bar {
    background: #f97316;
}

.chart-summary strong {
    color: #111827;
}

@media (max-width: 700px){.chart-row {
        grid-template-columns: 1fr;
        gap: 8px;
    }.chart-value {
        text-align: left;
    }.chart-summary {
        flex-direction: column;
        align-items: flex-start;
        gap: 6px;
    }}.line-chart {
    width: 100%;
    max-width: 720px;
    height: auto;
    margin-top: 20px;
    overflow: visible;
}

.chart-axis {
    stroke: #9ca3af;
    stroke-width: 2;
}

.prediction-line {
    fill: none !important;
    stroke: #f97316;
    stroke-width: 4;
    stroke-linecap: round;
    stroke-linejoin: round;
}

.prediction-point {
    stroke: #ffffff;
    stroke-width: 2;
}

.history-point {
    fill: #2563eb;
}

.projected-point {
    fill: #f97316;
}

.chart-legend {
    font-size: 11px;
    fill: #475569;
    font-weight: 700;
}

.legend-line {
    height: 4px;
    stroke-width: 0;
}

.legend-line--average {
    fill: #f59e0b;
}

.legend-line--trend {
    fill: #2563eb;
}

.legend-text {
    font-size: 11px;
    font-weight: 700;
    fill: #475569;
}

.point-value {
    font-size: 11px;
    font-weight: 700;
    fill: #111827;
}

.point-label {
    font-size: 11px;
    font-weight: 700;
    fill: #6b7280;
}

.axis-value {
    font-size: 11px;
    font-weight: 700;
    fill: #6b7280;
}

.chart-grid-line {
    stroke: #e5e7eb;
    stroke-width: 1;
}

.prediction-summary {
    margin-top: 24px;
}

.prediction-summary .macro-row {
    padding: 8px 0;
}

.chart-summary {
    margin-top: 20px;
    padding-top: 16px;
    border-top: 1px solid #e5e7eb;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.right-progress-wrapper {
    margin-top: 26px;
    margin-bottom: 24px;
    padding-top: 8px;
}

.right-progress-row {
    display: grid;
    grid-template-columns: 90px 1fr 90px;
    align-items: center;
    gap: 18px;
}

.right-progress-label {
    font-weight: 700;
    color: #374151;
}

.right-progress-value {
    text-align: right;
    font-weight: 800;
    color: #111827;
}

.prediction-line-chart-card {
    margin-top: 32px;
    padding: 28px;
}

.prediction-line-chart-card .panel-header {
    padding: 0 0 18px 0;
    margin-bottom: 18px;
}

.chart-caption {
    margin-top: 0;
    margin-bottom: 28px;
    line-height: 1.6;
}

.prediction-layout {
    display: grid;
    grid-template-columns: minmax(0, 1.15fr) minmax(380px, 0.85fr);
    gap: 24px;
    align-items: start;
}

.prediction-control-panel,
.prediction-result-panel {
    width: 100%;
}

.prediction-card-body {
    padding: 28px;
}

.prediction-card-body .prediction-description {
    margin-bottom: 28px;
}

.prediction-card-body .form-group {
    margin-bottom: 20px;
}

.prediction-card-body .form-actions {
    margin-top: 10px;
}

.prediction-result-body {
    padding: 24px;
}

.prediction-result-body .prediction-stats {
    margin-top: 0;
    margin-bottom: 28px;
    gap: 22px;
}

.prediction-result-body .prediction-summary {
    margin-top: 28px;
}

.prediction-result-body .macro-row {
    padding: 10px 0;
}

@media (max-width: 1050px){.prediction-layout {
        grid-template-columns: 1fr;
    }}.admin-section-body {
    padding: 24px;
}

.admin-table-panel {
    margin-top: 24px;
}

.admin-table-wrapper {
    width: 100%;
    overflow-x: auto;
    padding: 0 24px 24px 24px;
}

.admin-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 14px;
}

.admin-table th, .admin-table td {
    padding: 14px 12px;
    border-bottom: 1px solid #e5e7eb;
    text-align: left;
}

.admin-table th {
    background: #f9fafb;
    color: #374151;
    font-weight: 800;
    text-transform: uppercase;
    font-size: 12px;
    letter-spacing: 0.04em;
}

.admin-table td {
    color: #111827;
}

.admin-table tr:hover {
    background: #f9fafb;
}

.success-box {
    margin-top: 18px;
    padding: 18px;
    border-radius: 14px;
    background: #ecfdf5;
    border: 1px solid #bbf7d0;
    color: #065f46;
}

.danger-action {
    margin-top: 18px;
    border: none;
    border-radius: 12px;
    padding: 12px 18px;
    background: #dc2626;
    color: #ffffff;
    font-weight: 800;
    cursor: pointer;
}

.danger-action:hover {
    background: #b91c1c;
}

.error-page-wrapper {
    min-height: calc(100vh - 80px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 40px;
}

.error-page-panel {
    max-width: 620px;
    width: 100%;
    padding: 48px;
    text-align: center;
}

.error-code {
    width: 96px;
    height: 96px;
    margin: 0 auto 24px auto;
    border-radius: 28px;
    background: #f59e0b;
    color: #111827;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 34px;
    font-weight: 900;
    box-shadow: 0 18px 35px rgba(245, 158, 11, 0.25);
}

.error-page-panel h1 {
    margin-bottom: 14px;
    font-size: 36px;
    color: #111827;
}

.error-page-panel p {
    margin: 0 auto;
    max-width: 500px;
    color: #6b7280;
    line-height: 1.7;
    font-size: 16px;
}

.error-actions {
    margin-top: 30px;
    display: flex;
    justify-content: center;
    gap: 14px;
}

.average-trend-line {
    stroke: #f59e0b;
    stroke-width: 2;
    stroke-dasharray: 8 6;
    opacity: 0.9;
}

.trend-line {
    stroke: #2563eb;
    stroke-width: 2;
    stroke-dasharray: 6 6;
    opacity: 0.75;
}

.average-trend-label, .trend-line-label {
    fill: #111827;
    font-size: 12px;
    font-weight: 800;
}

.trend-line-label {
    fill: #2563eb;
}

.flash-messages {
    margin-bottom: 1rem;
}

.flash-message {
    padding: 0.85rem 1rem;
    border-radius: 10px;
    background: #fee2e2;
    color: #991b1b;
    font-weight: 700;
    border: 1px solid #fecaca;
}

.flash-messages {
    margin-bottom: 1rem;
}

.flash-message {
    padding: 0.85rem 1rem;
    border-radius: 10px;
    background: #fee2e2;
    color: #991b1b;
    font-weight: 700;
    border: 1px solid #fecaca;
}

.inline-delete-form {
    display: inline;
}

.meal-actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-top: 0.75rem;
}

.delete-button {
    background: none;
    border: none;
    padding: 0;
    cursor: pointer;
    font: inherit;
}

.nav-link.active {
    font-weight: 700;
}

/* Smoother panel header layout */
.panel-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1.25rem;
}

.panel-header h3 {
    margin: 0;
    font-size: 1.25rem;
    font-weight: 800;
    color: #0f172a;
}

/* Makes the small Add Meal link look controlled */
.panel-header .table-link {
    font-size: 0.95rem;
    font-weight: 700;
    text-decoration: none;
    color: #2563eb;
}

.panel-header .table-link:hover {
    text-decoration: underline;
}

/* Clean empty state for meals/panels */
.empty-state {
    padding: 2.5rem 1.5rem;
    border: 1px dashed #cbd5e1;
    border-radius: 18px;
    background: #f8fafc;
    text-align: center;
    color: #64748b;
}

.empty-state h4 {
    margin: 0 0 0.4rem;
    font-size: 1.15rem;
    font-weight: 800;
    color: #0f172a;
}

.empty-state p {
    margin: 0 auto 1.25rem;
    max-width: 420px;
    font-size: 0.98rem;
    line-height: 1.5;
    color: #64748b;
}

/* Smaller button inside empty states */
.empty-state .primary-action {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: auto;
    padding: 0.75rem 1.1rem;
    border-radius: 12px;
    font-size: 0.95rem;
    font-weight: 800;
    text-decoration: none;
}

/* Keeps meal action buttons neat when meals exist */
.meal-actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-top: 0.75rem;
}

.inline-delete-form {
    display: inline;
}

.delete-button {
    background: none;
    border: none;
    padding: 0;
    cursor: pointer;
    font: inherit;
}

.delete-button:hover {
    text-decoration: underline;
}


/*Table styles */

.main-content table,
.dashboard-page table,
.panel table {
    width: 100%;
    border-collapse: separate;
    border-spacing: 0;
    background: #ffffff;
    border: 1px solid #e5e7eb;
    border-radius: 18px;
    overflow: hidden;
    box-shadow: 0 12px 30px rgba(15, 23, 42, 0.06);
}

/* Table header */
.main-content thead,
.dashboard-page thead,
.panel thead {
    background: #f8fafc;
}

.main-content th,
.dashboard-page th,
.panel th {
    padding: 1.1rem 1.25rem;
    text-align: left;
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #475569;
    border-bottom: 1px solid #e5e7eb;
}

.main-content td,
.dashboard-page td,
.panel td {
    padding: 1.15rem 1.25rem;
    font-size: 0.98rem;
    color: #0f172a;
    border-bottom: 1px solid #eef2f7;
    vertical-align: middle;
}

.main-content tbody tr:last-child td,
.dashboard-page tbody tr:last-child td,
.panel tbody tr:last-child td {
    border-bottom: none;
}

.main-content tbody tr,
.dashboard-page tbody tr,
.panel tbody tr {
    transition: background 0.2s ease, transform 0.2s ease;
}

.main-content tbody tr:hover,
.dashboard-page tbody tr:hover,
.panel tbody tr:hover {
    background: #f8fafc;
}

.table-link {
    color: #2563eb;
    font-weight: 800;
    text-decoration: none;
    transition: color 0.2s ease, opacity 0.2s ease;
}

.table-link:hover {
    color: #1d4ed8;
    text-decoration: underline;
}

.delete-button,
button.table-link {
    background: none;
    border: none;
    padding: 0;
    font: inherit;
    color: #dc2626;
    font-weight: 800;
    cursor: pointer;
}

.delete-button:hover,
button.table-link:hover {
    color: #b91c1c;
    text-decoration: underline;
}

td form {
    display: inline;
}

td .table-link + .table-link,
td .table-link + form,
td form + .table-link {
    margin-left: 0.45rem;
}

.status-pill,
.saved-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.35rem 0.75rem;
    border-radius: 999px;
    background: #eef2ff;
    color: #3730a3;
    font-size: 0.82rem;
    font-weight: 800;
}

td span {
    max-width: max-content;
}

/* Empty table row */
.main-content td[colspan],
.dashboard-page td[colspan],
.panel td[colspan] {
    padding: 2.5rem 1.25rem;
    text-align: center;
    color: #64748b;
    font-size: 1rem;
    font-weight: 700;
    background: #ffffff;
}

/* Page headings above tables */
.main-content h1,
.dashboard-page h1 {
    margin-bottom: 1.5rem;
    font-size: clamp(2rem, 4vw, 2.6rem);
    font-weight: 900;
    letter-spacing: -0.04em;
    color: #0f172a;
}

/* Add breathing room around standalone tables */
.main-content > table,
.dashboard-page > table,
.panel > table {
    margin-top: 1rem;
}

/* Mobile table handling */
@media (max-width: 800px) {
    .main-content table,
    .dashboard-page table,
    .panel table {
        display: block;
        overflow-x: auto;
        white-space: nowrap;
        border-radius: 14px;
    }

    .main-content th,
    .dashboard-page th,
    .panel th,
    .main-content td,
    .dashboard-page td,
    .panel td {
        padding: 0.9rem 1rem;
    }
}

/* Home Buttons */

.home-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-top: 2rem;
    flex-wrap: wrap;
}

.home-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 130px;
    padding: 0.85rem 1.25rem;
    border-radius: 14px;
    font-size: 0.98rem;
    font-weight: 800;
    text-decoration: none;
    transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
}

.home-button.primary {
    background: #2563eb;
    color: #ffffff;
    box-shadow: 0 10px 24px rgba(37, 99, 235, 0.25);
}

.home-button.primary:hover {
    background: #1d4ed8;
    transform: translateY(-2px);
}

.home-button.secondary {
    background: #ffffff;
    color: #2563eb;
    border: 1px solid #cbd5e1;
}

.home-button.secondary:hover {
    background: #f8fafc;
    transform: translateY(-2px);
}